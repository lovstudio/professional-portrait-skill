name: Validate skill

on:
  push:
  pull_request:

jobs:
  validate:
    uses: skill-publisher/dev-skills/.github/workflows/validate-skill.yml@main
    with:
      skill_name: lov-professional-portrait
